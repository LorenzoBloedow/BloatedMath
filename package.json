{
  "jest": {
    "preset": "ts-jest",
    "useESM": true
  },
  "scripts": {
    "test": "jest --watchAll --collectCoverage"
  },
  "devDependencies": {
    "@types/jest": "^28.1.3",
    "jest": "^28.1.2",
    "ts-jest": "^28.0.5",
    "typescript": "^4.7.4"
  }
}
